# CABT Railway v5.1.3 · 덱빌더 복원

## 복원된 기능

- Railway 공개 페이지에서 덱빌더 버튼 표시
- 카드 이름·ID·기술 텍스트 검색
- 카드 종류 필터
- 클릭 및 드래그앤드롭 추가·제거
- 60장 및 동명 카드 4장 제한 검사
- 프리셋 덱 불러오기 후 수정
- 덱 저장·불러오기·삭제
- 저장한 덱으로 AI 대전, 빠른 매칭, 친구 방 참가

## 저장 방식

공개 서버에서 사용자 덱을 공용 `/data` 파일에 저장하지 않습니다. 각 덱은 해당 브라우저의 `localStorage`에 저장되고, 대전을 시작할 때 60장 카드 ID만 서버에 전달됩니다. 서버는 카드 존재 여부, 60장 수량, 동명 카드 4장 제한을 다시 검증합니다.

- 다른 사용자에게 덱이 보이지 않습니다.
- Railway Volume 용량을 사용하지 않습니다.
- 같은 브라우저와 같은 Railway 도메인에서만 저장 덱이 유지됩니다.
- 브라우저 사이트 데이터 삭제, 시크릿 창 종료, 도메인 변경 시 저장 덱이 사라질 수 있습니다.
- 다른 기기나 다른 브라우저로 자동 동기화되지는 않습니다.

## 호환성

v5.1.2의 다음 수정은 그대로 유지됩니다.

- PvP 1경기에 플레이어 2명 참가
- 큐 대기자는 워커 슬롯 미사용
- AI·리플레이 워커를 반환한 뒤 PvP 큐 참가
- heartbeat 및 접속 종료 후 워커 자동 해제
